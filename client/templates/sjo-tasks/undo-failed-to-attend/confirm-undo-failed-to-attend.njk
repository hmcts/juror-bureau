{% extends "layouts/default.njk" %}

{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "govuk/components/inset-text/macro.njk" import govukInsetText %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% block page_title %}{{ serviceName }} - SJO Tasks - Confirm undo failed to attend{% endblock %}
{% block page_identifier %}SJO Tasks - Confirm undo failed to attend{% endblock %}

{% block beforeContent %}

  {{ govukBackLink({
    text: "Back",
    href: backLinkUrl,
    attributes: {
      id: "backLinkAnchor"
    }
  }) }}

{% endblock %}

{% block content %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <form action="{{ postUrl }}" method="POST" id="confirmUncompleteForm">
      
        <h1 class="govuk-heading-xl">Undo failed to attend</h1>

        <div class="govuk-body">
          You're undoing a failed to attend status for {{ jurorsAmount }} juror{% if jurorsAmount > 1 %}s{% endif %}.
        </div>

        {{ govukInsetText({
          text: "Their juror status will revert to ‘Responded’ and they must continue their jury service."
        }) }}

        <div class="govuk-button-group">
          {{ govukButton({
            text: "Uncomplete service",
            type: "submit",
            attributes: {
              id: "continueButton"
            }
          }) }}
          <a href="{{ cancelUrl }}" class="govuk-link" id="cancelAnchor">Cancel</a>
        </div>
        
        <input type="hidden" name="_csrf" id="_csrf" value="{{ csrftoken }}">
      </form>
    </div>
  </div>

{% endblock %}